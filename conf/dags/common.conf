include "../secrets/local_airflow.conf"

FileSystem {
    path = "VacStorage"
}

Dags {
    RefreshMatViews {
        schedule = "30 0 * * *"
    }

    DeleteData {
        schedule = "30 10 * * *"
        rawData  = 30
    }

    BackupDB {
        schedule  = ""
        keepDumps = 3
    }
}