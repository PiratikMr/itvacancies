name: Scala ETL CI

on:
  push:
    branches: ["master", "develop"]
    paths:
      - 'services/etl/**'
      - '.github/workflows/scala-ci.yml'
    
jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./services/etl

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'sbt'
        cache-dependency-path: ./services/etl/build.sbt

    - name: Install sbt
      uses: sbt/setup-sbt@v1

    - name: Run Tests
      run: sbt test