package org.example.finder.config

case class FinderFileConfig(
                             vacsLimit: Int,
                             vacsPerPage: Int,
                             publicationTime: String
                           )
