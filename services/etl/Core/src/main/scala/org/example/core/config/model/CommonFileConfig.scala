package org.example.core.config.model

case class CommonFileConfig(
                             rawPartitions: Int,
                             updateLimit: Int,
                             apiBaseUrl: String
                           )
